meta {
  name: Login
  type: http
  seq: 1
}

post {
  url: {{url}}user/login
  body: json
  auth: none
}

body:json {
  {
    "email":"admin@gmail.com",
    "password":"pwd123",
    "role":"admin"
  }
}

script:post-response {
  if (res.status === 200 && res.body.data) {
    bru.setVar('token', res.body.data.records.token);
  }
}
